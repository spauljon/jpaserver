server_name: "local-nats"

jetstream: {
  store_dir: "/var/lib/nats/js"
  max_mem_store: 512Mi
  max_file_store: 10Gi
}

# Basic auth is off for local dev. Add users if you need.
http: 8222
listen: 0.0.0.0:4222
